# Copy to .env — defaults to /api (Vite proxy in dev, same-origin in production).

VITE_API_URL=/api

# Vercel (Project → Settings → Environment Variables)
# Add VITE_API_URL=/api for Production, Preview, and Development builds.
# No backend secrets are required for the demo API.
