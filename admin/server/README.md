# Legacy demo API (unused by default)

The admin UI is wired to the shared **`backend/`** API (`VITE_API_URL` → port 5000).

This folder is kept only for reference / emergency `npm run dev:demo-api`.
Safe to delete once you no longer need the old JSONB demo store.
